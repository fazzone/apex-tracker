trigger:
- master

pool:
  vmImage: 'windows-2019'

steps:
  - task: CmdLine@2
    inputs:
      script: |
        choco install bazel python --no-progress
        bazel build --compilation_mode=opt //:dist
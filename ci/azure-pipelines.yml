trigger:
- master

pool:
  vmImage: 'windows-2019'

steps:
  - task: CmdLine@2
    inputs:
      script: |
        set
        choco install bazel python --no-progress
        bazel build --python_path=%PYTHON_HOME% --compilation_mode=opt //:dist